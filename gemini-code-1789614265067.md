# Tài liệu Yêu cầu Dự án: HR Portal - Quản lý Phê duyệt Nghỉ phép

## 1. Tổng quan dự án
Ứng dụng web nội bộ giúp số hóa quy trình xin nghỉ phép của nhân viên và quy trình phê duyệt của cấp quản lý, giảm thiểu giấy tờ và tăng tính minh bạch.

## 2. Các Vai trò Người dùng (Roles)
- **Nhân viên (Employee):** Gửi đơn xin nghỉ, xem lịch sử đơn đã nộp, theo dõi số ngày phép còn lại.
- **Quản lý (Manager):** Xem danh sách đơn do nhân viên gửi đến, thực hiện phê duyệt hoặc từ chối kèm lý do.

## 3. Tính năng cốt lõi (Core Features)

### 3.1. Phía Nhân viên
- **Đăng nhập:** Xác thực tài khoản cơ bản.
- **Tạo đơn nghỉ phép:** 
  - Chọn loại phép (Phép năm, ốm, việc riêng, không lương).
  - Chọn thời gian: Từ ngày... đến ngày... (có tính buổi sáng/chiều).
  - Nhập lý do xin nghỉ.
- **Theo dõi trạng thái:** Xem danh sách đơn đã gửi với các trạng thái rõ ràng: *Chờ duyệt (Pending)*, *Đã duyệt (Approved)*, *Từ chối (Rejected)*.

### 3.2. Phía Quản lý
- **Danh sách chờ duyệt:** Hiển thị toàn bộ đơn nghỉ phép của nhân viên cấp dưới chưa được xử lý.
- **Phê duyệt / Từ chối:** 
  - Bấm nút "Duyệt" để hoàn tất quy trình.
  - Bấm nút "Từ chối" và bắt buộc phải nhập lý do từ chối.

## 4. Quy tắc nghiệp vụ (Business Rules)
- Ngày kết thúc nghỉ phép không được nhỏ hơn ngày bắt đầu.
- Nhân viên không thể sửa hoặc xóa đơn sau khi quản lý đã bấm duyệt hoặc từ chối.

## 5. Cấu trúc Dữ liệu Dự kiến (Database Schema)
- **Bảng Users (Người dùng):** `id`, `name`, `email`, `role` (employee/manager), `remaining_leave_days`.
- **Bảng LeaveRequests (Đơn nghỉ phép):** 
  - `id`
  - `employee_id` (Khóa ngoại liên kết với Users)
  - `leave_type` (Phép năm, ốm,...)
  - `start_date`, `end_date`
  - `reason`
  - `status` (Pending, Approved, Rejected)
  - `reject_reason`